{
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vpr -r build && vp check && vpr -r test",
    "prepare": "vp config",
    "release:zag": "vp exec -F faisceau-zag bumpp --all -c 'release faisceau-zag@' -t faisceau-zag@",
    "release:ui": "vp exec -F faisceau-ui bumpp --all -c 'release faisceau-ui@' -t faisceau-ui@"
  },
  "devDependencies": {
    "@vitest/browser-playwright": "catalog:",
    "bumpp": "catalog:",
    "playwright": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "12.5.1",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
